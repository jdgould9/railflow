import {
  __commonJS
} from "./chunk-HKJ2B2AA.js";

// node_modules/time-stamp/index.js
var require_time_stamp = __commonJS({
  "node_modules/time-stamp/index.js"(exports, module) {
    var dateRegex = /(?=(YYYY|YY|MM|DD|HH|mm|ss|ms))\1([:\/]*)/g;
    var timespan = {
      YYYY: ["getFullYear", 4],
      YY: ["getFullYear", 2],
      MM: ["getMonth", 2, 1],
      // getMonth is zero-based, thus the extra increment field
      DD: ["getDate", 2],
      HH: ["getHours", 2],
      mm: ["getMinutes", 2],
      ss: ["getSeconds", 2],
      ms: ["getMilliseconds", 3]
    };
    var timestamp = function(str, date, utc) {
      if (typeof str !== "string") {
        date = str;
        str = "YYYY-MM-DD";
      }
      if (!date) date = /* @__PURE__ */ new Date();
      return str.replace(dateRegex, function(match, key, rest) {
        var args = timespan[key];
        var name = args[0];
        var chars = args[1];
        if (utc === true) name = "getUTC" + name.slice(3);
        var val = "00" + String(date[name]() + (args[2] || 0));
        return val.slice(-chars) + (rest || "");
      });
    };
    timestamp.utc = function(str, date) {
      return timestamp(str, date, true);
    };
    module.exports = timestamp;
  }
});
export default require_time_stamp();
//# sourceMappingURL=time-stamp.js.map
